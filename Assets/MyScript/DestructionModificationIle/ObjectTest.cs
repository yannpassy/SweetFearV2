using UnityEngine;
using System.Collections;

public class ObjectTest : MonoBehaviour {
    public GameObject RocherTest;
	// Use this for initialization
	void Start () {
        Instantiate(RocherTest);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
